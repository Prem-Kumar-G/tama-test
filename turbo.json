{
  "$schema": "https://turborepo.org/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**"]
    }
  },
  "globalEnv": ["TAMAGUI_TARGET", "IGNORE_TS_CONFIG_PATHS", "TAMAGUI_DISABLE_WARN_DYNAMIC_LOAD"]
}
